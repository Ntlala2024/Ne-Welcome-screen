<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeWelcomeScreen"
        tools:targetApi="31">
        <activity
            android:name=".Scorescreen"
            android:exported="false"
            android:label="@string/title_activity_scorescreen"
            android:theme="@style/Theme.NeWelcomeScreen" />
        <activity
            android:name=".Flashcard"
            android:exported="false"
            android:label="@string/title_activity_flashcard"
            android:theme="@style/Theme.NeWelcomeScreen" />
        <activity
            android:name=".Flachcard"
            android:exported="false"
            android:label="@string/title_activity_flachcard"
            android:theme="@style/Theme.NeWelcomeScreen" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.NeWelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>